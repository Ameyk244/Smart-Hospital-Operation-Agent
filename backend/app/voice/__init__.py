"""Streaming voice input (experimental, branch `voice`). See docs/voice.md
for the architecture, the transport/VAD/STT decisions and why, and the
convergence point with the text pipeline (`app.api.routes.chat.
handle_chat_message`)."""
